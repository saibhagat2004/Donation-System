{
  "name": "donation-system",
  "version": "1.0.0",
  "description": "A full-stack donation management system",
  "main": "backend/server.js",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "start": "cd backend && npm start",
    "build": "npm install && npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend",
    "render-postbuild": "npm run build",
    "postinstall": "npm install --prefix backend && npm install --prefix frontend"
  },
  "keywords": ["donation", "charity", "mern", "fullstack"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^6.0.0"
  }
}